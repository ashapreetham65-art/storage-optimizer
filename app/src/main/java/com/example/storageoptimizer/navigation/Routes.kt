package com.example.storageoptimizer.navigation

// Add new screen routes here as the app grows.
// Keep them as constants so typos are caught at compile time.
object Routes {
    const val HOME    = "home"
    const val GALLERY = "gallery"
}